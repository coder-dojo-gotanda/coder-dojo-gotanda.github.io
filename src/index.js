import ReactDOM from 'react-dom';
import React from 'react';

const dom = document.getElementById('root');

const Main = (_props) => {
  return <h1>CoderDojo Gotanda</h1>
};

ReactDOM.render(<Main />, dom);
